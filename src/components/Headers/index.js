export { ConnectedHeader as Header } from './Header';

import * as actions from './actions/HeaderActions';

import reducer from './reducer/HeaderReducer';

export const HeaderActions = actions;
export const HeaderReducer = reducer;
