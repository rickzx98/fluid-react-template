import pt from 'prop-types';
import r from 'react';

export const PropTypes = pt;
export const React = r;

export { Label, Modal, FontAwesome, ResponsiveButton } from '../../common/';
export { connect } from 'react-redux';
export { getLabel } from '../../../utils/';